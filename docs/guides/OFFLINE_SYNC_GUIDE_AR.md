# دليل المزامنة الأوفلاين - BridgeCore

## 📚 جدول المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المعمارية](#المعمارية)
- [المفاهيم الأساسية](#المفاهيم-الأساسية)
- [مرجع API](#مرجع-api)
- [دليل التنفيذ](#دليل-التنفيذ)
- [حل التعارضات](#حل-التعارضات)
- [أفضل الممارسات](#أفضل-الممارسات)
- [أمثلة عملية](#أمثلة-عملية)
- [حل المشاكل](#حل-المشاكل)

---

## 🌟 نظرة عامة

نظام المزامنة الأوفلاين في BridgeCore هو نظام شامل للتزامن المصمم للتطبيقات التي تحتاج للعمل بشكل سلس مع أو بدون اتصال بالإنترنت.

### المميزات الرئيسية

✅ **معمارية Push/Pull**: رفع التغييرات المحلية وتنزيل تحديثات السيرفر
✅ **كشف التعارضات**: كشف تلقائي للتعارضات مع استراتيجيات متعددة للحل
✅ **حل التبعيات**: يتعامل مع العلاقات المعقدة بين السجلات
✅ **المعالجة الدفعية**: معالجة فعالة للتغييرات المتعددة
✅ **المزامنة التزايدية**: فقط ما تغير منذ آخر مزامنة
✅ **دعم عدة أجهزة**: كل جهاز يحتفظ بحالة مزامنة مستقلة
✅ **فلترة النماذج**: مزامنة البيانات ذات الصلة فقط
✅ **عزل التينانت**: عزل كامل للبيانات لكل تينانت

### حالات الاستخدام

1. **تطبيق المبيعات**: مندوبو المبيعات يعملون أوفلاين ويزامنون عند العودة
2. **تطبيق التوصيل**: السائقون يحدثون حالة التوصيل أوفلاين
3. **تطبيق المستودعات**: تحديثات المخزون في مناطق ذات اتصال ضعيف
4. **خدمة ميدانية**: الفنيون يعملون في مواقع بعيدة
5. **تطبيق CRM**: فريق المبيعات يحدث العملاء المحتملين أوفلاين

---

## 🏗️ المعمارية

### نظرة عامة على النظام

```
┌─────────────────────────────────────────────────────────────────┐
│                    تطبيق الموبايل/الويب                        │
│                    (Offline-First)                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ قاعدة بيانات │  │خدمة المزامنة │  │  واجهة      │         │
│  │   SQLite     │◄─┤              ├─►│  المستخدم    │         │
│  └──────────────┘  └───────┬──────┘  └──────────────┘         │
│                            │                                    │
└────────────────────────────┼────────────────────────────────────┘
                             │ HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BridgeCore API                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         نقاط نهاية المزامنة الأوفلاين                    │  │
│  │  /api/v1/offline-sync/push      (رفع)                    │  │
│  │  /api/v1/offline-sync/pull      (سحب)                    │  │
│  │  /api/v1/offline-sync/resolve-conflicts (حل التعارضات)  │  │
│  │  /api/v1/offline-sync/state     (الحالة)                 │  │
│  │  /api/v1/offline-sync/reset     (إعادة ضبط)             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### تدفق البيانات

#### Push (رفع التغييرات المحلية)

```
1. المستخدم يقوم بتغييرات أوفلاين
   ↓
2. حفظ التغييرات في SQLite المحلي بحالة "pending_sync"
   ↓
3. عند الاتصال، التطبيق ينادي /push مع كل التغييرات المعلقة
   ↓
4. BridgeCore يعالج التغييرات دفعات:
   - ترتيب حسب التبعيات
   - استبدال المعرفات المحلية بمعرفات السيرفر
   - كشف التعارضات
   - تنفيذ create/update/delete على Odoo
   ↓
5. إرجاع النتائج مع خريطة المعرفات
   ↓
6. التطبيق يحدث قاعدة البيانات المحلية:
   - ربط المعرفات المحلية بمعرفات السيرفر
   - وضع علامة "synced" على التغييرات
   - معالجة التعارضات إن وجدت
```

#### Pull (تنزيل تحديثات السيرفر)

```
1. التطبيق ينادي /pull مع last_event_id
   ↓
2. BridgeCore يستعلم Odoo عن الأحداث الجديدة:
   - WHERE id > last_event_id
   - فلترة حسب نماذج app_type
   - ترتيب تصاعدي
   ↓
3. إرجاع الأحداث الجديدة مع بيانات السيرفر
   ↓
4. التطبيق يطبق التغييرات على قاعدة البيانات المحلية:
   - إنشاء سجلات جديدة
   - تحديث سجلات موجودة
   - حذف سجلات محذوفة
   ↓
5. تحديث last_event_id للمزامنة القادمة
```

---

## 💡 المفاهيم الأساسية

### 1. حالة المزامنة (Sync State)

كل جهاز يحتفظ بحالة مزامنة خاصة به:

```json
{
  "device_id": "iphone-abc123",
  "user_id": 1,
  "last_event_id": 1250,
  "last_sync_time": "2025-11-24T10:30:00Z",
  "total_syncs": 150,
  "app_type": "sales_app"
}
```

**الغرض**: تتبع ما تم مزامنته لتجنب التكرار

### 2. التغييرات المحلية (Local Changes)

التغييرات التي تمت أوفلاين يتم تتبعها محلياً:

```json
{
  "local_id": "local_uuid_1",
  "action": "create",
  "model": "sale.order",
  "record_id": null,
  "data": {
    "partner_id": 42,
    "date_order": "2025-11-24"
  },
  "local_timestamp": "2025-11-24T10:30:00Z",
  "version": 1
}
```

**الغرض**: قائمة انتظار للتغييرات للرفع عند الاتصال

### 3. كشف التعارضات (Conflict Detection)

التعارضات تحدث عندما:
- التغيير المحلي حدث قبل تغيير السيرفر
- سجل السيرفر تم تعديله بواسطة مستخدم آخر
- الطوابع الزمنية لا تتطابق

**مثال**:
```
المستخدم أ (أوفلاين): يغير رقم هاتف العميل الساعة 10:30
المستخدم ب (أونلاين): يغير رقم هاتف العميل الساعة 10:35
المستخدم أ (أونلاين): يحاول المزامنة الساعة 10:40 ← تعارض!
```

### 4. استراتيجيات حل التعارضات

```python
SERVER_WINS    # بيانات السيرفر لها الأولوية (افتراضي)
CLIENT_WINS    # بيانات العميل تكتب فوق السيرفر
MANUAL         # إرجاع التعارض للمستخدم لحله
MERGE          # دمج النسختين
NEWEST_WINS    # التغيير الأحدث يفوز
```

### 5. فلترة حسب نوع التطبيق

كل نوع تطبيق يزامن فقط النماذج ذات الصلة:

```python
APP_TYPE_MODELS = {
    "sales_app": [
        "sale.order",
        "sale.order.line",
        "res.partner",
        "product.product"
    ],
    "delivery_app": [
        "stock.picking",
        "stock.move",
        "res.partner"
    ],
    # ... المزيد من أنواع التطبيقات
}
```

**الغرض**: تقليل نقل البيانات ووقت المزامنة

---

## 📡 مرجع API

### الرابط الأساسي

```
https://api.bridgecore.com/api/v1/offline-sync
```

### المصادقة

جميع نقاط النهاية تتطلب مصادقة JWT:

```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

### نقاط النهاية

#### 1. رفع التغييرات المحلية

**POST** `/push`

رفع التغييرات المحلية إلى السيرفر.

**جسم الطلب**:
```json
{
  "device_id": "iphone-abc123",
  "changes": [
    {
      "local_id": "local_uuid_1",
      "action": "create",
      "model": "sale.order",
      "record_id": null,
      "data": {
        "partner_id": 42,
        "order_line": []
      },
      "local_timestamp": "2025-11-24T10:30:00Z",
      "version": 1
    }
  ],
  "conflict_strategy": "server_wins",
  "stop_on_error": false
}
```

**الاستجابة**:
```json
{
  "success": true,
  "total": 10,
  "succeeded": 9,
  "failed": 0,
  "conflicts": 1,
  "results": [...],
  "id_mapping": {
    "local_uuid_1": 789
  },
  "server_timestamp": "2025-11-24T10:31:00Z"
}
```

**حد الطلبات**: 100 طلب/دقيقة

---

#### 2. سحب تحديثات السيرفر

**POST** `/pull`

تنزيل التغييرات الجديدة من السيرفر.

**جسم الطلب**:
```json
{
  "device_id": "iphone-abc123",
  "app_type": "sales_app",
  "last_event_id": 1250,
  "limit": 100,
  "include_payload": true
}
```

**الاستجابة**:
```json
{
  "success": true,
  "has_updates": true,
  "new_events_count": 25,
  "events": [
    {
      "event_id": 1251,
      "model": "sale.order",
      "record_id": 456,
      "action": "write",
      "timestamp": "2025-11-24T10:30:00Z",
      "data": {...}
    }
  ],
  "last_event_id": 1275,
  "server_timestamp": "2025-11-24T10:31:00Z"
}
```

**حد الطلبات**: 200 طلب/دقيقة

---

## 🚀 دليل التنفيذ

### دورة المزامنة الكاملة

```typescript
// 1. تهيئة خدمة المزامنة
const syncService = new OfflineSyncService({
  baseUrl: 'https://api.bridgecore.com',
  deviceId: 'iphone-abc123',
  appType: 'sales_app',
});

// 2. تسجيل الدخول
await syncService.login(email, password);

// 3. بدء المزامنة التلقائية (كل 30 ثانية)
syncService.startBackgroundSync();

// 4. مزامنة يدوية عند طلب المستخدم
async function manualSync() {
  try {
    // الخطوة 1: رفع التغييرات المحلية
    const pushResult = await syncService.push();
    console.log(`تم رفع ${pushResult.succeeded} تغيير`);

    // الخطوة 2: معالجة التعارضات إن وجدت
    if (pushResult.conflicts > 0) {
      await handleConflicts(pushResult);
    }

    // الخطوة 3: سحب تحديثات السيرفر
    const pullResult = await syncService.pull();
    console.log(`تم سحب ${pullResult.new_events_count} حدث`);

    // الخطوة 4: تطبيق على قاعدة البيانات المحلية
    await applyEventsToDatabase(pullResult.events);

    // الخطوة 5: تحديث الواجهة
    notifyUser('تمت المزامنة بنجاح');

  } catch (error) {
    console.error('فشلت المزامنة:', error);
    notifyUser('فشلت المزامنة. سيتم إعادة المحاولة تلقائياً.');
  }
}
```

---

## ⚔️ حل التعارضات

### استراتيجيات الحل

#### 1. السيرفر يفوز (افتراضي)

```json
{
  "conflict_strategy": "server_wins"
}
```

- **استخدم عندما**: بيانات السيرفر دائماً صحيحة
- **النتيجة**: التغيير المحلي يتم تجاهله
- **آمن لـ**: الإعدادات، التكوينات

#### 2. العميل يفوز

```json
{
  "conflict_strategy": "client_wins"
}
```

- **استخدم عندما**: بيانات العميل دائماً صحيحة
- **النتيجة**: بيانات السيرفر تُكتب فوقها
- **آمن لـ**: تفضيلات المستخدم، المسودات

#### 3. الحل اليدوي

```json
{
  "conflict_strategy": "manual"
}
```

- **استخدم عندما**: المستخدم يجب أن يقرر
- **النتيجة**: التعارض يُرجع للعميل
- **آمن لـ**: البيانات الحرجة للأعمال

---

## ✅ أفضل الممارسات

### 1. تكرار المزامنة

```typescript
// مزامنة خلفية كل 30 ثانية
setInterval(async () => {
  if (navigator.onLine && !isSyncing) {
    await syncService.syncOnce();
  }
}, 30000);

// مزامنة فورية للتغييرات الحرجة
async function saveCriticalChange(data) {
  await saveOfflineChange('create', 'sale.order', data);
  await syncService.push(); // مزامنة فورية
}
```

### 2. معالجة الأخطاء

```typescript
try {
  await syncService.push();
} catch (error) {
  if (error.code === 'NETWORK_ERROR') {
    // إعادة المحاولة لاحقاً
    scheduleRetry();
  } else if (error.code === 'CONFLICT') {
    // معالجة التعارضات
    await handleConflicts(error.conflicts);
  } else {
    // تسجيل الخطأ
    logger.error('فشلت المزامنة', error);
  }
}
```

### 3. التحديثات المتفائلة

```typescript
// تحديث الواجهة فوراً
updateUI(newData);

// حفظ محلياً
await saveOfflineChange('update', 'res.partner', newData);

// مزامنة في الخلفية
syncService.push().catch(error => {
  // إرجاع الواجهة إذا فشلت المزامنة
  revertUI();
  showError('فشل التحديث. حاول مرة أخرى.');
});
```

---

## 📱 أمثلة عملية

### مثال Flutter

سيتم تضمينه في القسم التالي من الأمثلة...

---

## 🔧 حل المشاكل

### المشكلة: التغييرات لا تتزامن

**الحلول**:
1. تحقق من اتصال الإنترنت
2. تأكد من صلاحية JWT token
3. تحقق من حالة المزامنة
4. راجع التغييرات المعلقة في قاعدة البيانات المحلية
5. تحقق من سجلات API

### المشكلة: تعارضات متكررة

**الحلول**:
1. زامن بشكل أكثر تكراراً
2. استخدم الإصدارات (version field)
3. غير استراتيجية التعارض إلى `newest_wins`
4. نفذ حل التعارضات على مستوى الحقل
5. ثقف المستخدمين حول التحرير الأوفلاين

---

## 🎯 الخلاصة

المزامنة الأوفلاين في BridgeCore توفر حلاً متكاملاً لبناء تطبيقات offline-first:

✅ معمارية **Push/Pull** للمزامنة ثنائية الاتجاه
✅ **حل التعارضات** مع استراتيجيات متعددة
✅ **مزامنة تزايدية** للكفاءة
✅ **دعم عدة أجهزة** بحالات مستقلة
✅ **معالجة دفعية** للأداء
✅ **فلترة النماذج** لتقليل نقل البيانات
✅ **عزل التينانت** للأمان

### الخطوات القادمة

1. 📖 اقرأ [مرجع API](#مرجع-api)
2. 💻 راجع [الأمثلة العملية](#أمثلة-عملية)
3. 🚀 ابدأ التنفيذ في تطبيقك
4. 📞 اتصل بالدعم للمساعدة

---

**صُنع بـ ❤️ من فريق BridgeCore**

للدعم: support@bridgecore.com
التوثيق: https://docs.bridgecore.com
GitHub: https://github.com/geniustep/BridgeCore
